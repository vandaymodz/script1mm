local uis = game:GetService("UserInputService")
local runService = game:GetService("RunService")

-- Tạo giao diện chính
local screenGui = Instance.new("ScreenGui", game.CoreGui)
screenGui.Name = "VANDAYMODZ_UI"

-- Biến toàn cục
local dragging = false
local dragInput, dragStart, startPos
local minimized = false

-- Thanh tiêu đề
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(0, 400, 0, 30)
titleBar.Position = UDim2.new(0.5, -200, 0.3, 0)
titleBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
titleBar.BorderSizePixel = 0
titleBar.Parent = screenGui
titleBar.Active = true
titleBar.Draggable = true

local titleLabel = Instance.new("TextLabel", titleBar)
titleLabel.Text = "VANDAYMODZ"
titleLabel.Size = UDim2.new(1, -40, 1, 0)
titleLabel.Position = UDim2.new(0, 10, 0, 0)
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.BackgroundTransparency = 1
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 14
titleLabel.TextXAlignment = Enum.TextXAlignment.Left

-- Nút thu nhỏ
local minimizeBtn = Instance.new("TextButton", titleBar)
minimizeBtn.Text = "-"
minimizeBtn.Size = UDim2.new(0, 30, 0, 30)
minimizeBtn.Position = UDim2.new(1, -35, 0, 0)
minimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
minimizeBtn.BorderSizePixel = 0
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 18

-- Khung menu
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 400, 0, 250)
menu.Position = UDim2.new(0, 0, 1, 0)
menu.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
menu.BorderSizePixel = 0
menu.Parent = titleBar

-- Khung tab
local tabFrame = Instance.new("Frame", menu)
tabFrame.Size = UDim2.new(0, 100, 1, 0)
tabFrame.Position = UDim2.new(0, 0, 0, 0)
tabFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)

-- Khung nội dung
local contentFrame = Instance.new("Frame", menu)
contentFrame.Size = UDim2.new(1, -100, 1, 0)
contentFrame.Position = UDim2.new(0, 100, 0, 0)
contentFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
contentFrame.ClipsDescendants = true

-- Hàm tạo toggle
local function createToggle(parent, text, default, callback)
    local toggleFrame = Instance.new("Frame", parent)
    toggleFrame.Size = UDim2.new(1, -20, 0, 25)
    toggleFrame.BackgroundTransparency = 1
    
    local toggleLabel = Instance.new("TextLabel", toggleFrame)
    toggleLabel.Text = text
    toggleLabel.Size = UDim2.new(0.7, 0, 1, 0)
    toggleLabel.Position = UDim2.new(0, 0, 0, 0)
    toggleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleLabel.BackgroundTransparency = 1
    toggleLabel.Font = Enum.Font.Gotham
    toggleLabel.TextSize = 14
    toggleLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local toggleButton = Instance.new("TextButton", toggleFrame)
    toggleButton.Size = UDim2.new(0.3, 0, 1, 0)
    toggleButton.Position = UDim2.new(0.7, 0, 0, 0)
    toggleButton.Text = default and "ON" or "OFF"
    toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleButton.BackgroundColor3 = default and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(170, 0, 0)
    toggleButton.Font = Enum.Font.GothamBold
    toggleButton.TextSize = 14
    
    local toggled = default
    
    toggleButton.MouseButton1Click:Connect(function()
        toggled = not toggled
        toggleButton.Text = toggled and "ON" or "OFF"
        toggleButton.BackgroundColor3 = toggled and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(170, 0, 0)
        if callback then callback(toggled) end
    end)
    
    return toggleButton
end

-- Hàm tạo slider
local function createSlider(parent, text, min, max, default, callback)
    local sliderFrame = Instance.new("Frame", parent)
    sliderFrame.Size = UDim2.new(1, -20, 0, 50)
    sliderFrame.BackgroundTransparency = 1
    
    local sliderLabel = Instance.new("TextLabel", sliderFrame)
    sliderLabel.Text = text
    sliderLabel.Size = UDim2.new(1, 0, 0, 20)
    sliderLabel.Position = UDim2.new(0, 0, 0, 0)
    sliderLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    sliderLabel.BackgroundTransparency = 1
    sliderLabel.Font = Enum.Font.Gotham
    sliderLabel.TextSize = 14
    sliderLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local valueLabel = Instance.new("TextLabel", sliderFrame)
    valueLabel.Text = tostring(default)
    valueLabel.Size = UDim2.new(0, 40, 0, 20)
    valueLabel.Position = UDim2.new(1, -40, 0, 0)
    valueLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Font = Enum.Font.Gotham
    valueLabel.TextSize = 14
    valueLabel.TextXAlignment = Enum.TextXAlignment.Right
    
    local sliderBack = Instance.new("Frame", sliderFrame)
    sliderBack.Size = UDim2.new(1, 0, 0, 10)
    sliderBack.Position = UDim2.new(0, 0, 0, 30)
    sliderBack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    sliderBack.BorderSizePixel = 0
    
    local sliderFill = Instance.new("Frame", sliderBack)
    sliderFill.Size = UDim2.new((default - min)/(max - min), 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    sliderFill.BorderSizePixel = 0
    
    local sliderButton = Instance.new("TextButton", sliderBack)
    sliderButton.Size = UDim2.new(0, 20, 0, 20)
    sliderButton.Position = UDim2.new((default - min)/(max - min), 0, 0, -5)
    sliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    sliderButton.Text = ""
    sliderButton.BorderSizePixel = 0
    
    local dragging = false
    
    sliderButton.MouseButton1Down:Connect(function()
        dragging = true
    end)
    
    uis.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    
    sliderButton.MouseMoved:Connect(function()
        if dragging then
            local x = math.clamp((uis:GetMouseLocation().X - sliderBack.AbsolutePosition.X) / sliderBack.AbsoluteSize.X, 0, 1)
            local value = math.floor(min + (max - min) * x)
            sliderFill.Size = UDim2.new(x, 0, 1, 0)
            sliderButton.Position = UDim2.new(x, 0, 0, -5)
            valueLabel.Text = tostring(value)
            if callback then callback(value) end
        end
    end)
    
    return sliderButton
end

-- Hàm tạo tab
local function createTab(name)
    local tabBtn = Instance.new("TextButton", tabFrame)
    tabBtn.Size = UDim2.new(1, 0, 0, 40)
    tabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    tabBtn.Text = name
    tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    tabBtn.Font = Enum.Font.Gotham
    tabBtn.TextSize = 14
    tabBtn.BorderSizePixel = 0

    tabBtn.MouseEnter:Connect(function()
        tabBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    end)
    
    tabBtn.MouseLeave:Connect(function()
        tabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    end)

    local content = Instance.new("ScrollingFrame", contentFrame)
    content.Size = UDim2.new(1, 0, 1, 0)
    content.BackgroundTransparency = 1
    content.ScrollBarThickness = 5
    content.Visible = false
    content.AutomaticCanvasSize = Enum.AutomaticSize.Y
    
    local layout = Instance.new("UIListLayout", content)
    layout.Padding = UDim.new(0, 5)
    
    tabBtn.MouseButton1Click:Connect(function()
        for _, c in pairs(contentFrame:GetChildren()) do
            if c:IsA("ScrollingFrame") then c.Visible = false end
        end
        content.Visible = true
    end)

    return content
end

-- Tạo các tab
local aimTab = createTab("AIM")
local espTab = createTab("ESP")
local miscTab = createTab("MISC")

-- Nội dung tab AIM
local aimbotEnabled = false
local aimSilentEnabled = false
local bypassEnabled = false
local fovCircleSize = 100
local fovAngle = 90

createToggle(aimTab, "Aimbot", false, function(state)
    aimbotEnabled = state
    print("Aimbot:", state)
    -- Thêm code aimbot tại đây
end)

createToggle(aimTab, "Aim Silent", false, function(state)
    aimSilentEnabled = state
    print("Aim Silent:", state)
    -- Thêm code aim silent tại đây
end)

createToggle(aimTab, "Bypass", false, function(state)
    bypassEnabled = state
    print("Bypass:", state)
    -- Thêm code bypass tại đây
end)

createSlider(aimTab, "FOV Circle", 10, 500, 100, function(value)
    fovCircleSize = value
    print("FOV Circle Size:", value)
    -- Thêm code FOV Circle tại đây
end)

createSlider(aimTab, "FOV Angle", 1, 360, 90, function(value)
    fovAngle = value
    print("FOV Angle:", value)
    -- Thêm code FOV Angle tại đây
end)

-- Hiển thị tab AIM mặc định
aimTab.Visible = true

-- Thu nhỏ/gỡ bỏ
minimizeBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    menu.Visible = not minimized
    minimizeBtn.Text = minimized and "+" or "-"
end)
-- Nội dung tab ESP
local espLineEnabled = false
local espBoxEnabled = false
local nameHealthEnabled = false
local espDistanceEnabled = false
local chamsEnabled = false

-- Tạo các toggle cho ESP
createToggle(espTab, "ESP Line", false, function(state)
    espLineEnabled = state
    print("ESP Line:", state)
    -- Code ESP Line
    if state then
        -- Bật ESP Line
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player ~= game:GetService("Players").LocalPlayer then
                local line = Instance.new("Line")
                line.Thickness = 1
                line.Color3 = player.Team == game:GetService("Players").LocalPlayer.Team 
                    and Color3.fromRGB(0, 255, 0) 
                    or Color3.fromRGB(255, 0, 0)
                line.Parent = screenGui
                
                game:GetService("RunService").RenderStepped:Connect(function()
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local rootPart = player.Character.HumanoidRootPart
                        line.From = game:GetService("Workspace").CurrentCamera.ViewportPointToRay(
                            game:GetService("Workspace").CurrentCamera.CFrame.Position.X,
                            game:GetService("Workspace").CurrentCamera.CFrame.Position.Y,
                            game:GetService("Workspace").CurrentCamera.CFrame.Position.Z
                        ).Origin
                        line.To = rootPart.Position
                    end
                end)
            end
        end
    else
        -- Tắt ESP Line
        for _, obj in pairs(screenGui:GetChildren()) do
            if obj:IsA("Line") then
                obj:Destroy()
            end
        end
    end
end)

createToggle(espTab, "ESP Box", false, function(state)
    espBoxEnabled = state
    print("ESP Box:", state)
    -- Code ESP Box
    if state then
        -- Bật ESP Box
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player ~= game:GetService("Players").LocalPlayer and player.Character then
                local box = Instance.new("BoxHandleAdornment")
                box.Size = Vector3.new(3, 6, 3)
                box.Color3 = player.Team == game:GetService("Players").LocalPlayer.Team 
                    and Color3.fromRGB(0, 255, 0) 
                    or Color3.fromRGB(255, 0, 0)
                box.Transparency = 0.5
                box.Adornee = player.Character:FindFirstChild("HumanoidRootPart")
                box.AlwaysOnTop = true
                box.ZIndex = 10
                box.Parent = player.Character.HumanoidRootPart
            end
        end
    else
        -- Tắt ESP Box
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                for _, obj in pairs(player.Character.HumanoidRootPart:GetChildren()) do
                    if obj:IsA("BoxHandleAdornment") then
                        obj:Destroy()
                    end
                end
            end
        end
    end
end)

createToggle(espTab, "Name + Health", false, function(state)
    nameHealthEnabled = state
    print("Name + Health:", state)
    -- Code Name + Health
    if state then
        -- Bật Name + Health
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player ~= game:GetService("Players").LocalPlayer and player.Character then
                local billboard = Instance.new("BillboardGui")
                billboard.Size = UDim2.new(0, 100, 0, 50)
                billboard.StudsOffset = Vector3.new(0, 3, 0)
                billboard.Adornee = player.Character:FindFirstChild("HumanoidRootPart")
                billboard.AlwaysOnTop = true
                billboard.Parent = player.Character.HumanoidRootPart
                
                local nameLabel = Instance.new("TextLabel", billboard)
                nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
                nameLabel.Text = player.Name
                nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                nameLabel.BackgroundTransparency = 1
                nameLabel.Font = Enum.Font.GothamBold
                
                local healthLabel = Instance.new("TextLabel", billboard)
                healthLabel.Size = UDim2.new(1, 0, 0.5, 0)
                healthLabel.Position = UDim2.new(0, 0, 0.5, 0)
                healthLabel.Text = "HP: "..tostring(math.floor(player.Character.Humanoid.Health))
                healthLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                healthLabel.BackgroundTransparency = 1
                healthLabel.Font = Enum.Font.Gotham
                
                player.Character.Humanoid.HealthChanged:Connect(function()
                    healthLabel.Text = "HP: "..tostring(math.floor(player.Character.Humanoid.Health))
                end)
            end
        end
    else
        -- Tắt Name + Health
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                for _, obj in pairs(player.Character.HumanoidRootPart:GetChildren()) do
                    if obj:IsA("BillboardGui") then
                        obj:Destroy()
                    end
                end
            end
        end
    end
end)

createToggle(espTab, "ESP Distance", false, function(state)
    espDistanceEnabled = state
    print("ESP Distance:", state)
    -- Code ESP Distance
    if state then
        -- Bật ESP Distance
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player ~= game:GetService("Players").LocalPlayer and player.Character then
                local billboard = Instance.new("BillboardGui")
                billboard.Size = UDim2.new(0, 100, 0, 20)
                billboard.StudsOffset = Vector3.new(0, 4, 0)
                billboard.Adornee = player.Character:FindFirstChild("HumanoidRootPart")
                billboard.AlwaysOnTop = true
                billboard.Parent = player.Character.HumanoidRootPart
                
                local distanceLabel = Instance.new("TextLabel", billboard)
                distanceLabel.Size = UDim2.new(1, 0, 1, 0)
                distanceLabel.BackgroundTransparency = 1
                distanceLabel.Font = Enum.Font.Gotham
                distanceLabel.TextSize = 14
                
                game:GetService("RunService").RenderStepped:Connect(function()
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local distance = (player.Character.HumanoidRootPart.Position - 
                            game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
                        distanceLabel.Text = tostring(math.floor(distance)).." studs"
                        distanceLabel.TextColor3 = distance < 50 and Color3.fromRGB(255, 0, 0) or
                                                  distance < 100 and Color3.fromRGB(255, 165, 0) or
                                                  Color3.fromRGB(0, 255, 0)
                    end
                end)
            end
        end
    else
        -- Tắt ESP Distance
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                for _, obj in pairs(player.Character.HumanoidRootPart:GetChildren()) do
                    if obj:IsA("BillboardGui") and obj:FindFirstChild("TextLabel") then
                        obj:Destroy()
                    end
                end
            end
        end
    end
end)

createToggle(espTab, "Chams", false, function(state)
    chamsEnabled = state
    print("Chams:", state)
    -- Code Chams
    if state then
        -- Bật Chams
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player ~= game:GetService("Players").LocalPlayer and player.Character then
                for _, part in pairs(player.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        local highlight = Instance.new("Highlight")
                        highlight.FillColor = player.Team == game:GetService("Players").LocalPlayer.Team 
                            and Color3.fromRGB(0, 255, 0) 
                            or Color3.fromRGB(255, 0, 0)
                        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                        highlight.FillTransparency = 0.5
                        highlight.Parent = part
                    end
                end
            end
        end
    else
        -- Tắt Chams
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player.Character then
                for _, part in pairs(player.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        for _, obj in pairs(part:GetChildren()) do
                            if obj:IsA("Highlight") then
                                obj:Destroy()
                            end
                        end
                    end
                end
            end
        end
    end
end)

-- ESP Counter (Phiên bản hoàn thiện)
local espCounterFrame = Instance.new("Frame", espTab)
espCounterFrame.Size = UDim2.new(1, -20, 0, 80)
espCounterFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
espCounterFrame.BorderSizePixel = 0

-- Tạo hiệu ứng gradient cho counter
local gradient = Instance.new("UIGradient", espCounterFrame)
gradient.Rotation = 90
gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 20, 20)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 50, 50))
})

local playerLabel = Instance.new("TextLabel", espCounterFrame)
playerLabel.Text = "PLAYER: 0"
playerLabel.Size = UDim2.new(1, 0, 0, 25)
playerLabel.Position = UDim2.new(0, 0, 0, 5)
playerLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
playerLabel.BackgroundTransparency = 1
playerLabel.Font = Enum.Font.GothamBold
playerLabel.TextSize = 16
playerLabel.TextXAlignment = Enum.TextXAlignment.Center

local teamLabel = Instance.new("TextLabel", espCounterFrame)
teamLabel.Text = "TEAM: 0"
teamLabel.Size = UDim2.new(1, 0, 0, 20)
teamLabel.Position = UDim2.new(0, 0, 0, 35)
teamLabel.TextColor3 = Color3.fromRGB(100, 255, 100)  -- Xanh lá nhạt
teamLabel.BackgroundTransparency = 1
teamLabel.Font = Enum.Font.Gotham
teamLabel.TextSize = 14
teamLabel.TextXAlignment = Enum.TextXAlignment.Center

local enemyLabel = Instance.new("TextLabel", espCounterFrame)
enemyLabel.Text = "ENEMY: 0"
enemyLabel.Size = UDim2.new(1, 0, 0, 20)
enemyLabel.Position = UDim2.new(0, 0, 0, 55)
enemyLabel.TextColor3 = Color3.fromRGB(255, 100, 100)  -- Đỏ nhạt
enemyLabel.BackgroundTransparency = 1
enemyLabel.Font = Enum.Font.Gotham
enemyLabel.TextSize = 14
enemyLabel.TextXAlignment = Enum.TextXAlignment.Center

-- Cập nhật ESP Counter (Phiên bản nâng cao)
local function updateCounter()
    local players = game:GetService("Players"):GetPlayers()
    local localPlayer = game:GetService("Players").LocalPlayer
    local teamCount = 0
    local enemyCount = 0
    
    for _, player in pairs(players) do
        if player ~= localPlayer and player.Character then
            if player.Team == localPlayer.Team then
                teamCount = teamCount + 1
            else
                enemyCount = enemyCount + 1
            end
        end
    end
    
    -- Hiệu ứng khi thay đổi số lượng
    playerLabel.Text = "PLAYER: "..tostring(#players)
    teamLabel.Text = "TEAM: "..tostring(teamCount)
    enemyLabel.Text = "ENEMY: "..tostring(enemyCount)
    
    -- Đổi màu khi số lượng thay đổi
    if enemyCount > teamCount then
        espCounterFrame.BackgroundColor3 = Color3.fromRGB(50, 0, 0)  -- Đỏ đậm khi địch nhiều hơn
    elseif enemyCount == teamCount then
        espCounterFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 0)  -- Vàng khi cân bằng
    else
        espCounterFrame.BackgroundColor3 = Color3.fromRGB(0, 50, 0)  -- Xanh khi đồng đội nhiều hơn
    end
end

-- Kết nối sự kiện
game:GetService("Players").PlayerAdded:Connect(updateCounter)
game:GetService("Players").PlayerRemoving:Connect(updateCounter)
game:GetService("RunService").RenderStepped:Connect(updateCounter)

-- Thêm chức năng ESP Team Highlight
local teamHighlightEnabled = false
local teamHighlights = {}

createToggle(espTab, "Team Highlight", false, function(state)
    teamHighlightEnabled = state
    if state then
        -- Bật highlight team
        for _, player in pairs(game:GetService("Players"):GetPlayers()) do
            if player ~= game:GetService("Players").LocalPlayer and player.Character then
                local highlight = Instance.new("Highlight")
                highlight.FillColor = player.Team == game:GetService("Players").LocalPlayer.Team 
                    and Color3.fromRGB(0, 100, 255)  -- Xanh dương cho đồng đội
                    or Color3.fromRGB(255, 100, 0)    -- Cam cho địch
                highlight.OutlineTransparency = 0.5
                highlight.FillTransparency = 0.3
                highlight.Parent = player.Character
                teamHighlights[player] = highlight
            end
        end
    else
        -- Tắt highlight team
        for player, highlight in pairs(teamHighlights) do
            highlight:Destroy()
        end
        teamHighlights = {}
    end
end)

-- Tự động cập nhật khi người chơi thay đổi team
game:GetService("Players").PlayerChangedTeam:Connect(function(player)
    if teamHighlightEnabled and teamHighlights[player] then
        teamHighlights[player].FillColor = player.Team == game:GetService("Players").LocalPlayer.Team 
            and Color3.fromRGB(0, 100, 255)
            or Color3.fromRGB(255, 100, 0)
    end
    updateCounter()
end)
-- Nội dung tab MISC
local speedHackValue = 16
local bunnyHopEnabled = false
local antiAfkEnabled = false
local noClipEnabled = false

-- Speed Hack
createSlider(miscTab, "Speed Hack", 1, 100, 16, function(value)
    speedHackValue = value
    print("Speed Hack:", value)
    
    -- Áp dụng speed hack
    if game:GetService("Players").LocalPlayer.Character then
        local humanoid = game:GetService("Players").LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = value
        end
    end
end)

-- Bunny Hop
local bhopConn
createToggle(miscTab, "Bunny Hop", false, function(state)
    bunnyHopEnabled = state
    print("Bunny Hop:", state)
    
    if state then
        -- Bật Bunny Hop
        bhopConn = game:GetService("RunService").RenderStepped:Connect(function()
            if game:GetService("Players").LocalPlayer.Character then
                local humanoid = game:GetService("Players").LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if humanoid and humanoid.FloorMaterial ~= Enum.Material.Air then
                    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end
        end)
    else
        -- Tắt Bunny Hop
        if bhopConn then
            bhopConn:Disconnect()
            bhopConn = nil
        end
    end
end)

-- Anti AFK
createToggle(miscTab, "Anti AFK", false, function(state)
    antiAfkEnabled = state
    print("Anti AFK:", state)
    
    if state then
        -- Bật Anti AFK
        local vu = game:GetService("VirtualUser")
        game:GetService("Players").LocalPlayer.Idled:Connect(function()
            vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            wait(1)
            vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        end)
    end
end)

-- No Clip
local noclipConn
createToggle(miscTab, "No Clip", false, function(state)
    noClipEnabled = state
    print("No Clip:", state)
    
    if state then
        -- Bật No Clip
        noclipConn = game:GetService("RunService").Stepped:Connect(function()
            if game:GetService("Players").LocalPlayer.Character then
                for _, part in pairs(game:GetService("Players").LocalPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    else
        -- Tắt No Clip
        if noclipConn then
            noclipConn:Disconnect()
            noclipConn = nil
        end
    end
end)

-- Auto Clicker (Bonus)
local autoClickerEnabled = false
local clickerConn
createToggle(miscTab, "Auto Clicker", false, function(state)
    autoClickerEnabled = state
    print("Auto Clicker:", state)
    
    if state then
        -- Bật Auto Clicker
        clickerConn = game:GetService("RunService").RenderStepped:Connect(function()
            if game:GetService("Players").LocalPlayer:GetMouse().Target then
                mouse1click()
            end
        end)
    else
        -- Tắt Auto Clicker
        if clickerConn then
            clickerConn:Disconnect()
            clickerConn = nil
        end
    end
end)

-- Cập nhật tốc độ khi respawn
game:GetService("Players").LocalPlayer.CharacterAdded:Connect(function(character)
    wait(1) -- Đợi load character
    if speedHackValue > 16 then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = speedHackValue
        end
    end
end)
-- Sửa lỗi hiển thị tab
local function showTab(tab)
    for _, child in pairs(contentFrame:GetChildren()) do
        if child:IsA("ScrollingFrame") then
            child.Visible = false
        end
    end
    tab.Visible = true
end

-- Kích hoạt lại các tab button
local aimTabBtn = tabFrame:FindFirstChild("AIM")
local espTabBtn = tabFrame:FindFirstChild("ESP")
local miscTabBtn = tabFrame:FindFirstChild("MISC")

if aimTabBtn then
    aimTabBtn.MouseButton1Click:Connect(function()
        showTab(aimTab)
    end)
end

if espTabBtn then
    espTabBtn.MouseButton1Click:Connect(function()
        showTab(espTab)
    end)
end

if miscTabBtn then
    miscTabBtn.MouseButton1Click:Connect(function()
        showTab(miscTab)
    end)
end

-- Đảm bảo tab AIM hiển thị mặc định
showTab(aimTab)

-- Thêm hiệu ứng chuyển tab
local function updateTabColors(selectedTab)
    for _, tabBtn in pairs(tabFrame:GetChildren()) do
        if tabBtn:IsA("TextButton") then
            if tabBtn == selectedTab then
                tabBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
                tabBtn.TextColor3 = Color3.fromRGB(0, 200, 255)
            else
                tabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
                tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end
end

-- Kết nối sự kiện cho các tab button
if aimTabBtn then
    aimTabBtn.MouseButton1Click:Connect(function()
        updateTabColors(aimTabBtn)
    end)
end

if espTabBtn then
    espTabBtn.MouseButton1Click:Connect(function()
        updateTabColors(espTabBtn)
    end)
end

if miscTabBtn then
    miscTabBtn.MouseButton1Click:Connect(function()
        updateTabColors(miscTabBtn)
    end)
end

-- Cập nhật màu tab mặc định
updateTabColors(aimTabBtn)

-- Thêm tính năng đóng/mở bằng phím tắt
uis.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed then
        if input.KeyCode == Enum.KeyCode.RightShift then
            minimized = not minimized
            menu.Visible = not minimized
            minimizeBtn.Text = minimized and "+" or "-"
        end
    end
end)

-- Hiển thị thông báo khi load UI
local notification = Instance.new("TextLabel", screenGui)
notification.Text = "VANDAYMODZ UI Đã Được Tải! (Nhấn RightShift để ẩn/hiện)"
notification.Size = UDim2.new(0, 400, 0, 30)
notification.Position = UDim2.new(0.5, -200, 0.1, 0)
notification.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
notification.TextColor3 = Color3.fromRGB(255, 255, 255)
notification.Font = Enum.Font.GothamBold
notification.TextSize = 14
notification.ZIndex = 100

-- Tự động ẩn thông báo sau 5 giây
delay(5, function()
    notification:Destroy()
end)